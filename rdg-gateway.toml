listen_addr = "0.0.0.0"
listen_port = 3443
server_name = "WIN-8QEC20TIHO4"

[tls]
auto_generate = true

[database]
url = "sqlite://C:/src/rdg-gateway/rdg-gateway.db?mode=rwc"

[auth]
# spn = "HTTP/gateway.domain.com"    # Auto-derived from server_name if not set
# keytab_path = "/etc/rdg-gateway/krb5.keytab"  # Linux/macOS only
open_mode = true  # Accept any NTLM without validation (testing only!)

[telemetry]
otlp_endpoint = "http://localhost:4317"
service_name = "rdg-gateway"
enabled = true

[webui]
enabled = false
# gateway_url = "https://gateway.example.com:3443"
